deployment:
  # -- The name of the company that makes the deployment
  company: "company2"
  # -- The name of the use case that is being deployed
  network_name: "epidev"
  # -- The Quorum node public ip address (ingress Quorum node address)
  enode_address: ""
  # -- The Port of the Quorum node public address
  enode_address_port: "30303"
  # -- NAT address, used for firewall configuration (egress node ip)
  NAT: "1.2.3.4"
fullnameOverride: "quorum"

# enode://public_key@url:port